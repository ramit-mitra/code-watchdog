{
  "name": "code-watchdog",
  "version": "1.0.0",
  "description": "A simple app to check for filesystem changes and hook custom actions on it",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 0",
    "setup": "clear && npm install && eslint --init",
    "watch": "clear && node index.js",
    "dev": "clear && nodemon index.js"
  },
  "author": "Ramit Mitra",
  "repository": {
    "type": "git",
    "url": "https://github.com/ramit-mitra/code-watchdog.git"
  },
  "license": "MIT",
  "dependencies": {
    "node-watch": "^0.5.8",
    "shelljs": "^0.8.2"
  }
}